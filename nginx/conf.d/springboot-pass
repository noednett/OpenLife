server {
	listen 80;
	listen [::]:80;

	server_name noednett.openlife.server;

	location / {
		proxy_pass http://localhost:8080;
		proxy_set_header X-Real-PI $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header Host $http_host;
	}
}
